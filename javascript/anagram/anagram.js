var Anagram = function(word) {
	this.word = word;
	return 
}

Anagram.prototype.matches = function(list) {
	/*
	for ( var i = 0; i < list.length; i++ ) {
		// If the word char count does not match 
		// the word char count it is no worth pursuing
		if ( list[i].length !== this.word.length ) {
			continue;
		}

	}
	*/
};

module.export = Anagram;
